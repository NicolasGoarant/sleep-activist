<%# Hero Section with Video %>

<!-- Étoiles constellation en arrière-plan -->
<div class="stars" id="starsContainer"></div>

<script>
// Générer les étoiles de constellation au chargement
(function() {
  const starsContainer = document.getElementById('starsContainer');
  const starCount = 100; // 100 étoiles fixes qui scintillent

  for (let i = 0; i < starCount; i++) {
    const star = document.createElement('div');
    star.className = 'star';
    star.style.left = Math.random() * 100 + '%';
    star.style.top = Math.random() * 100 + '%';
    star.style.animationDelay = Math.random() * 3 + 's'; // Décalage aléatoire du scintillement
    starsContainer.appendChild(star);
  }
})();
</script>

<%= content_tag :section, class: "hero home-hero", style: "display: block;" do %>
  <%= content_tag :div, class: "hero-content", style: "max-width: 1200px; margin: 0 auto; display: grid; grid-template-columns: 1fr 1fr; gap: 4rem; align-items: center;" do %>
    <%= content_tag :div do %>
      <%= content_tag :div, "✨ Un média vivant • Podcast, Blog, Instagram", class: "hero-tag hide-on-mobile" %>
      <%= content_tag :h1 do %>
        Le média du <%= content_tag :span, "sommeil", class: "highlight" %>
      <% end %>
            <%= content_tag :p, class: "hero-subtitle" do %>
        Un média qui milite pour un sommeil libre, joyeux et réparateur.
        Sans injonctions. Sans culpabilisation. Avec curiosité, humour et engagement.
      <% end %>
      <%= content_tag :div, class: "cta-group" do %>
        <%= link_to "Découvrir l'écosystème", "#contenus", class: "cta-primary" %>
        <%= link_to "Nos principes", principes_path, class: "cta-secondary" %>
      <% end %>
    <% end %>

    <%# Vidéo Élise Lucet à droite %>
    <%= content_tag :div do %>
      <%= content_tag :div, style: "position: relative; padding-bottom: 56.25%; height: 0; overflow: hidden; border-radius: 20px; box-shadow: 0 20px 60px rgba(0,0,0,0.4);" do %>
        <%= content_tag :iframe, "",
            src: "https://www.youtube.com/embed/qBfHg9Hyehk",
            style: "position: absolute; top: 0; left: 0; width: 100%; height: 100%;",
            frameborder: "0",
            allow: "accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture",
            allowfullscreen: true %>
      <% end %>
      <%= content_tag :p, style: "color: var(--soft-lavender); font-size: 0.9rem; margin-top: 1rem; text-align: center; font-style: italic;" do %>
        📺 Nicolas Goarant chez Élise Lucet (France 2, 2021)
      <% end %>
    <% end %>
  <% end %>
<% end %>

<%# Mission Section - Philosophie du média - INVERSÉ %>
<%= content_tag :section, class: "mission", id: "mission", style: "padding-top: 10rem;" do %>
  <%= content_tag :div, class: "mission-container", style: "display: grid; grid-template-columns: 1fr 1fr; gap: 5rem; align-items: center;" do %>
    <%# VIDÉO À GAUCHE %>
    <%= content_tag :div, class: "mission-visual" do %>
      <%# Vidéo d'illustration %>
      <%= content_tag :div, style: "width: 100%; height: 100%; border-radius: 30px; overflow: hidden; position: relative; background: var(--deep-navy);" do %>
        <%= content_tag :video,
            autoplay: true,
            loop: true,
            muted: true,
            playsinline: true,
            style: "width: 100%; height: 100%; object-fit: cover; display: block;",
            class: "mission-video" do %>
          <%= tag :source, src: "/videos/femme_enfant_light.mp4", type: "video/mp4" %>
        <% end %>
      <% end %>
    <% end %>

    <%# TEXTE À DROITE %>
    <%= content_tag :div, class: "mission-text" do %>
      <%= content_tag :h2, "Un média doux... mais engagé" %>
      <%= content_tag :p do %>
        Le sommeil est partout dans nos vies, mais rarement considéré comme il le mérite.
        Souvent réduit à une donnée biologique ou à un problème médical, il est pourtant
        un marqueur social, culturel et émotionnel.
      <% end %>
      <%= content_tag :p do %>
        Les Sleep Activists proposent d'en changer le récit : parler du sommeil sans
        injonctions, sans moralisation, mais avec curiosité, humour et liberté de ton.
      <% end %>
      <%= content_tag :p, style: "color: var(--moon-yellow); font-weight: 500; font-size: 1.2rem; margin-top: 2rem;" do %>
        Plus qu'un média, Sleep Activists revendique une posture : celle du corps qui
        dit stop, de l'esprit qui a besoin de nuit, du temps qu'on reprend.
      <% end %>
      <%= content_tag :p, style: "margin-top: 2rem; font-style: italic;" do %>
        Nous croyons qu'un autre sommeil est possible : apaisé, désirable, collectif.
      <% end %>
    <% end %>
  <% end %>
<% end %>

<%# Community Section - À qui s'adresse le média %>
<%= content_tag :section, class: "mission", style: "background: linear-gradient(to bottom, rgba(26, 20, 100, 0.3), transparent);" do %>
  <%= content_tag :div, class: "section-header", style: "margin-bottom: 4rem;" do %>
    <%= content_tag :h2, "À qui s'adressent les Sleep Activists ?" %>
  <% end %>

  <%= content_tag :div, style: "max-width: 1200px; margin: 0 auto; display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 2rem;" do %>
    <%= content_tag :div, style: "text-align: center; padding: 2rem; background: rgba(255, 255, 255, 0.03); border-radius: 20px; border: 1px solid rgba(184, 181, 232, 0.2);" do %>
      <%= content_tag :div, "👶", style: "font-size: 3rem; margin-bottom: 1rem;" %>
      <%= content_tag :h3, "Aux parents épuisés", style: "color: var(--warm-cream); font-family: 'Crimson Pro', serif; margin-bottom: 1rem;" %>
      <%= content_tag :p, "Parce que la fatigue parentale mérite d'être reconnue sans culpabilité", style: "color: var(--soft-lavender);" %>
    <% end %>
    <%= content_tag :div, style: "text-align: center; padding: 2rem; background: rgba(255, 255, 255, 0.03); border-radius: 20px; border: 1px solid rgba(184, 181, 232, 0.2);" do %>
      <%= content_tag :div, "😴", style: "font-size: 3rem; margin-bottom: 1rem;" %>
      <%= content_tag :h3, "Aux insomniaques", style: "color: var(--warm-cream); font-family: 'Crimson Pro', serif; margin-bottom: 1rem;" %>
      <%= content_tag :p, "Pour sortir de l'isolement et comprendre que vous n'êtes pas seuls", style: "color: var(--soft-lavender);" %>
    <% end %>
    <%= content_tag :div, style: "text-align: center; padding: 2rem; background: rgba(255, 255, 255, 0.03); border-radius: 20px; border: 1px solid rgba(184, 181, 232, 0.2);" do %>
      <%= content_tag :div, "🐌", style: "font-size: 3rem; margin-bottom: 1rem;" %>
      <%= content_tag :h3, "Aux militants du repos", style: "color: var(--warm-cream); font-family: 'Crimson Pro', serif; margin-bottom: 1rem;" %>
      <%= content_tag :p, "Pour ceux qui revendiquent le droit de ralentir dans un monde qui accélère", style: "color: var(--soft-lavender);" %>
    <% end %>
    <%= content_tag :div, style: "text-align: center; padding: 2rem; background: rgba(255, 255, 255, 0.03); border-radius: 20px; border: 1px solid rgba(184, 181, 232, 0.2);" do %>
      <%= content_tag :div, "🌃", style: "font-size: 3rem; margin-bottom: 1rem;" %>
      <%= content_tag :h3, "Aux travailleurs de nuit", style: "color: var(--warm-cream); font-family: 'Crimson Pro', serif; margin-bottom: 1rem;" %>
      <%= content_tag :p, "Votre rythme est différent, mais votre sommeil compte tout autant", style: "color: var(--soft-lavender);" %>
    <% end %>
  <% end %>
<% end %>

<%# Ecosystem Section - L'écosystème complet %>
<%= content_tag :section, class: "content-grid", id: "contenus" do %>
  <%= content_tag :div, class: "section-header" do %>
    <%= content_tag :h2, "Un écosystème complet" %>
    <%= content_tag :p, "Podcast, blog, réseaux sociaux, livre, événements" %>
  <% end %>
  <%= content_tag :div, class: "cards-grid" do %>
    <%= link_to podcast_path, style: "text-decoration: none;" do %>
      <%= content_tag :div, class: "content-card", style: "cursor: pointer;" do %>
        <%= content_tag :div, "🎙️", class: "card-image" %>
        <%= content_tag :div, class: "card-content" do %>
          <%= content_tag :span, "Audio", class: "card-tag" %>
          <%= content_tag :h3, "Podcast \"La Sieste\"" %>
          <%= content_tag :p, "Conversations profondes avec chercheurs, artistes, citoyens. Penser le sommeil autrement." %>
          <%= content_tag :span, "→ Écouter", style: "color: var(--moon-yellow); text-decoration: none;" %>
        <% end %>
      <% end %>
    <% end %>
    <%= link_to blog_path, style: "text-decoration: none;" do %>
      <%= content_tag :div, class: "content-card", style: "cursor: pointer;" do %>
        <%= content_tag :div, "✍️", class: "card-image" %>
        <%= content_tag :div, class: "card-content" do %>
          <%= content_tag :span, "Écrit", class: "card-tag" %>
          <%= content_tag :h3, "Le blog" %>
          <%= content_tag :p, "Analyses, récits, témoignages. Comprendre les enjeux. Agir sans culpabiliser." %>
          <%= content_tag :span, "→ Lire", style: "color: var(--moon-yellow); text-decoration: none;" %>
        <% end %>
      <% end %>
    <% end %>
    <%= link_to "https://instagram.com/sleep_activists", target: "_blank", style: "text-decoration: none;" do %>
      <%= content_tag :div, class: "content-card", style: "cursor: pointer;" do %>
        <%= content_tag :div, "📱", class: "card-image" %>
        <%= content_tag :div, class: "card-content" do %>
          <%= content_tag :span, "Social", class: "card-tag" %>
          <%= content_tag :h3, "Instagram" %>
          <%= content_tag :p, "Carrousels pédagogiques, séries thématiques. Déconstruire le discours dominant." %>
          <%= content_tag :span, "→ Suivre", style: "color: var(--moon-yellow); text-decoration: none;" %>
        <% end %>
      <% end %>
    <% end %>
    <%= link_to "https://editionsdelaube.fr/catalogue_de_livres/le-sommeil-malmene/", target: "_blank", style: "text-decoration: none;" do %>
      <%= content_tag :div, class: "content-card", style: "cursor: pointer;" do %>
        <%= content_tag :div, "📚", class: "card-image" %>
        <%= content_tag :div, class: "card-content" do %>
          <%= content_tag :span, "Livre", class: "card-tag" %>
          <%= content_tag :h3, "Le Sommeil Malmené" %>
          <%= content_tag :p, "Le livre fondateur (2020). Retrouver le goût de la nuit. Éditions de l'Aube." %>
          <%= content_tag :span, "→ En savoir plus", style: "color: var(--moon-yellow); text-decoration: none;" %>
        <% end %>
      <% end %>
    <% end %>
    <%= link_to newsletters_path, style: "text-decoration: none;" do %>
      <%= content_tag :div, class: "content-card", style: "cursor: pointer;" do %>
        <%= content_tag :div, "💌", class: "card-image" %>
        <%= content_tag :div, class: "card-content" do %>
          <%= content_tag :span, "Newsletters", class: "card-tag" %>
          <%= content_tag :h3, "Sleep Stories" %>
          <%= content_tag :p, "Chaque lundi : une histoire inspirante, des clés pour agir, zéro moralisation." %>
          <%= content_tag :span, "→ S'abonner", style: "color: var(--moon-yellow); text-decoration: none;" %>
        <% end %>
      <% end %>
    <% end %>
    <%= content_tag :div, class: "content-card", style: "opacity: 0.7;" do %>
      <%= content_tag :div, "🤝", class: "card-image" %>
      <%= content_tag :div, class: "card-content" do %>
        <%= content_tag :span, "Bientôt", class: "card-tag" %>
        <%= content_tag :h3, "Ateliers & rencontres" %>
        <%= content_tag :p, "Dès 2026 : événements physiques. Penser le repos ensemble, créer du lien." %>
      <% end %>
    <% end %>
  <% end %>
<% end %>

<%# Stats Section - Chiffres clés %>
<%= content_tag :section, class: "stats" do %>
  <%= content_tag :div, class: "stats-grid" do %>
    <%= content_tag :div, class: "stat-card" do %>
      <%= content_tag :div, "2020", class: "stat-number" %>
      <%= content_tag :div, class: "stat-label" do %>
        Livre "Le Sommeil Malmené"<br>publié aux éditions de l'Aube
      <% end %>
    <% end %>
    <%= content_tag :div, class: "stat-card" do %>
      <%= content_tag :div, "15+", class: "stat-number" %>
      <%= content_tag :div, class: "stat-label" do %>
        Médias nationaux<br>(France 2, RTL, Brut, RFI...)
      <% end %>
    <% end %>
    <%= content_tag :div, class: "stat-card" do %>
      <%= content_tag :div, "4", class: "stat-number" %>
      <%= content_tag :div, class: "stat-label" do %>
        Épisodes de podcast<br>"La Sieste"
      <% end %>
    <% end %>
    <%= content_tag :div, class: "stat-card" do %>
      <%= content_tag :div, "2025", class: "stat-number" %>
      <%= content_tag :div, class: "stat-label" do %>
        Lancement du média<br>Sleep Activists
      <% end %>
    <% end %>
  <% end %>
<% end %>

<%# Press Section - Version Média Prestige %>
<section class="press-logos-container" id="presse">
  <div style="text-align: center; margin-bottom: 40px;">
    <p class="press-logos-title">Ils parlent de notre engagement</p>
  </div>

  <div class="press-logos-carousel">
    <div class="press-logos-track">

      <%# Liste unique de tes médias avec les noms de fichiers corrigés %>
      <% press_items = [
        ["france_2.png", "France 2", "https://www.youtube.com/watch?v=qBfHg9Hyehk"],
        ["france_culture.png", "France Culture", "https://www.radiofrance.fr/franceculture/podcasts/la-vie-mode-d-emploi/notre-sommeil-entre-l-intime-et-le-politique-8860750"],
        ["france_inter.png", "France Inter", "https://www.facebook.com/grandbienvousfasse/posts/retrouvez-le-podcast-de-l%C3%A9mission-sur-le-sommeil-avec-pierre-philip-nicolas-goar/1031683047245173/"],
        ["rtl.png", "RTL", "https://youtu.be/LtPFmPsjKak"],
        ["brut.png", "Brut", "https://youtu.be/Xqj8cnh1nak"],
        ["rfi.png", "RFI", "https://www.rfi.fr/fr/podcasts/8-milliards-de-voisins/20250609-les-petits-plaisirs-de-la-sieste"],
        ["ouest_france.png", "Ouest-France", "https://www.ouest-france.fr/sante/dormir-moins-de-7-heures-par-nuit-se-paye-tres-cher-un-specialiste-du-sommeil-sonne-l-alarme-7083917"],
        ["usbek_et_rica.png", "Usbek et Rica", "https://usbeketrica.com/fr/article/le-sommeil-est-une-maladie-politique"]
      ] %>

      <%# On affiche la liste deux fois pour créer l'effet de boucle infinie %>
      <% 2.times do %>
        <% press_items.each do |logo, name, url| %>
          <%= link_to url, target: "_blank", class: "press-logo-link" do %>
            <img src="/images/<%= logo %>" alt="<%= name %>" class="press-logo-img" loading="lazy">
          <% end %>
        <% end %>
      <% end %>

    </div>
  </div>

  <div style="text-align: center; margin-top: 4rem;">
    <%= link_to "Voir toutes nos interventions médias →", principes_path, class: "press-cta-link" %>
  </div>
</section>

<%# Newsletter CTA %>
<%= content_tag :section, class: "newsletter-cta", id: "newsletter" do %>
  <%= content_tag :div, class: "newsletter-box" do %>
    <%= content_tag :h2, "Rejoignez les Sleep Activists" %>
    <%= content_tag :p do %>
      Chaque lundi, recevez une Sleep Story inspirante, des extraits de podcast,
      des analyses sans moralisation et des pistes pour repenser votre sommeil.
    <% end %>
    <%= form_with url: "#", method: :post, class: "newsletter-form", local: true do |f| %>
      <%= f.email_field :email, placeholder: "Votre email", required: true %>
      <%= f.submit "S'inscrire" %>
    <% end %>
    <%= content_tag :p, style: "margin-top: 2rem; font-size: 0.9rem;" do %>
      💌 Pas de spam. Désinscription en un clic. Vos données restent privées.
    <% end %>
  <% end %>
<% end %>

<%# Final Quote Section %>
<%= content_tag :section, class: "mission", style: "padding: 6rem 5%; text-align: center;" do %>
  <%= content_tag :div, style: "max-width: 800px; margin: 0 auto;" do %>
    <%= content_tag :p, style: "font-family: 'Crimson Pro', serif; font-size: 2rem; font-style: italic; color: var(--moon-yellow); margin-bottom: 2rem;" do %>
      "Dans le sommeil, nous voyageons plus loin que dans le monde entier"
    <% end %>
    <%= content_tag :p, "— Fernando Pessoa", style: "color: var(--soft-lavender); font-size: 1.2rem;" %>
  <% end %>
<% end %>


<!-- Script pour effets ambiants sommeil -->
<script>
(function() {
  // Créer des étoiles filantes aléatoires
  function createShootingStar() {
    const star = document.createElement('div');
    star.className = 'shooting-star';
    star.textContent = '⭐';
    star.style.left = Math.random() * 50 + '%';
    star.style.top = Math.random() * 50 + '%';
    star.style.animation = `shooting ${2 + Math.random() * 2}s linear`;

    document.body.appendChild(star);

    setTimeout(() => star.remove(), 4000);
  }

  // Créer des lunes flottantes
  function createFloatingMoon() {
    const moon = document.createElement('div');
    moon.className = 'floating-moon';
    moon.textContent = ['🌙', '🌛', '🌜'][Math.floor(Math.random() * 3)];
    moon.style.left = Math.random() * 80 + 10 + '%';
    moon.style.top = Math.random() * 60 + '%';
    moon.style.animationDelay = Math.random() * 5 + 's';

    document.body.appendChild(moon);

    // Supprimer après animation
    setTimeout(() => moon.remove(), 25000);
  }

  // Lancer les effets périodiquement
  setInterval(createShootingStar, 20000); // Toutes les 20 secondes au lieu de 8
  setInterval(createFloatingMoon, 45000); // Toutes les 45 secondes au lieu de 15

  // Première vague
  setTimeout(createShootingStar, 5000);
  setTimeout(createFloatingMoon, 12000);
})();
</script>

<!-- Script parallaxe étoiles (effet onirique doux) -->
<script>
(function() {
  // Créer des étoiles fixes pour parallaxe
  function createBackgroundStars() {
    const container = document.body;
    const starCount = 40;

    for (let i = 0; i < starCount; i++) {
      const star = document.createElement('div');
      star.className = 'parallax-star';
      star.textContent = '·';
      star.style.cssText = `
        position: fixed;
        color: rgba(255, 255, 255, ${0.2 + Math.random() * 0.3});
        font-size: ${Math.random() * 2 + 1}px;
        left: ${Math.random() * 100}%;
        top: ${Math.random() * 100}%;
        pointer-events: none;
        z-index: 0;
        transition: transform 0.3s ease-out;
      `;
      star.dataset.speed = (0.05 + (i % 5) * 0.03).toString();
      container.appendChild(star);
    }
  }

  // Parallaxe au scroll (optimisé)
  let ticking = false;
  window.addEventListener('scroll', () => {
    if (!ticking) {
      window.requestAnimationFrame(() => {
        const scrolled = window.pageYOffset;
        const stars = document.querySelectorAll('.parallax-star');

        stars.forEach(star => {
          const speed = parseFloat(star.dataset.speed);
          star.style.transform = `translateY(${scrolled * speed}px)`;
        });

        ticking = false;
      });
      ticking = true;
    }
  });

  createBackgroundStars();
})();
</script>

<!-- Script mouton simple -->
<script>
(function() {
  // Fonction pour créer le mouton
  function showSheep() {
    const sheep = document.createElement('div');
    sheep.className = 'simple-sheep';
    sheep.textContent = '🐑';
    document.body.appendChild(sheep);

    // Ajouter l'événement clic
    sheep.addEventListener('click', () => handleSheepClick(sheep));

    // Supprimer après l'animation (18 secondes)
    setTimeout(() => {
      sheep.remove();
    }, 18000);
  }

  // Fonction au clic sur le mouton
  function handleSheepClick(sheepElement) {
    // Animation de saut
    sheepElement.classList.add('sheep-jump');

    // Créer des étoiles qui éclatent
    createSparkles(sheepElement);

    // Faire disparaître le mouton progressivement après le saut
    setTimeout(() => {
      sheepElement.style.transition = 'opacity 0.5s ease-out';
      sheepElement.style.opacity = '0';

      // Supprimer complètement après la disparition
      setTimeout(() => {
        sheepElement.remove();
      }, 500);
    }, 600);
  }

  // Fonction pour créer les étoiles au clic sur le mouton
  function createSparkles(sheepElement) {
    // Position fixe au centre de l'écran
    const centerX = window.innerWidth / 2;
    const centerY = window.innerHeight / 2;

    for (let i = 0; i < 8; i++) { // 8 étoiles au lieu de 6
      const sparkle = document.createElement('div');
      sparkle.textContent = ['✨', '💫', '⭐'][Math.floor(Math.random() * 3)];

      // Style complet en inline
      sparkle.style.cssText = `
        position: fixed;
        left: ${centerX}px;
        top: ${centerY}px;
        font-size: 1.5rem;
        pointer-events: none;
        z-index: 999;
        opacity: 0;
      `;

      const angle = (Math.PI * 2 * i) / 8;
      const distance = 100 + Math.random() * 60;
      const tx = Math.cos(angle) * distance;
      const ty = Math.sin(angle) * distance;

      sparkle.style.setProperty('--tx', `${tx}px`);
      sparkle.style.setProperty('--ty', `${ty}px`);

      // Animation manuelle
      sparkle.style.animation = 'sparkle-burst 0.8s ease-out forwards';

      document.body.appendChild(sparkle);
      setTimeout(() => sparkle.remove(), 800);
    }
  }

// Fonction pour les étincelles au clic
  function createClickSparkle(e) {
    // On récupère la position précise du clic
    const x = e.clientX;
    const y = e.clientY;

    for (let i = 0; i < 8; i++) {
      const sparkle = document.createElement('div');
      sparkle.className = 'click-sparkle';
      sparkle.textContent = ['✨', '⭐', '💫'][Math.floor(Math.random() * 3)];

      // Positionnement initial au point de clic
      sparkle.style.left = `${x}px`;
      sparkle.style.top = `${y}px`;

      // Direction aléatoire de l'explosion
      const tx = (Math.random() - 0.5) * 200;
      const ty = (Math.random() - 0.5) * 200;
      sparkle.style.setProperty('--tx', `${tx}px`);
      sparkle.style.setProperty('--ty', `${ty}px`);

      document.body.appendChild(sparkle);
      setTimeout(() => sparkle.remove(), 8000);
    }
  }

  // Fonction pour le message "Bonne nuit"
  // Fonction pour créer des Z indépendants (occasionnellement)
  function createFloatingZ() {
    const z = document.createElement('div');
    z.className = 'sleep-z';
    z.textContent = '💤';

    // Position aléatoire sur l'écran
    z.style.left = `${Math.random() * 80 + 10}%`;
    z.style.bottom = `${Math.random() * 30 + 10}%`;

    document.body.appendChild(z);
    setTimeout(() => z.remove(), 3000);
  }

  // Première apparition du mouton : 2 secondes après le chargement
  setTimeout(showSheep, 2000);

  // Réapparition du mouton toutes les 45 secondes
  setInterval(showSheep, 45000);

  // Z indépendants qui apparaissent occasionnellement (toutes les 20 secondes)
  setInterval(createFloatingZ, 20000);

  // Premier Z indépendant après 10 secondes
  setTimeout(createFloatingZ, 10000);
})();
</script>
