<div class="newsletter-index-page">
  <!-- En-tête -->
  <div class="newsletter-index-header">
    <h1 class="index-title">📬 Nos Newsletters</h1>
    <p class="index-subtitle">Le sommeil, enfin libéré — Éditions mensuelles</p>
    <p class="index-description">
      Découvre nos newsletters mensuelles : conseils, astuces DIY, témoignages et découvertes scientifiques
      pour transformer tes nuits. Sans culpabilité, avec joie et légèreté.
    </p>
  </div>

  <!-- Liste des newsletters -->
  <div class="newsletters-grid">
    <% @newsletters.each do |newsletter| %>
      <article class="newsletter-card">
        <div class="card-badge">🌙 Édition #<%= newsletter[:number] %></div>
        <h2 class="card-title"><%= newsletter[:title] %></h2>
        <p class="card-date"><%= newsletter[:date] %></p>
        <p class="card-excerpt"><%= newsletter[:excerpt] %></p>
        <div class="card-highlights">
          <% newsletter[:tags].each do |tag| %>
            <span class="highlight-tag"><%= tag %></span>
          <% end %>
        </div>
        <%= link_to "Lire la newsletter →", newsletter_path(newsletter[:id]), class: "card-link" %>
      </article>
    <% end %>
  </div>

  <!-- Call to action -->
  <div class="newsletter-cta-box">
    <h3>📧 Inscris-toi à la newsletter</h3>
    <p>Reçois chaque mois nos conseils, astuces et découvertes directement dans ta boîte mail.</p>
    <%= link_to "S'inscrire gratuitement", new_subscriber_path, class: "cta-button" %>
  </div>
</div>
