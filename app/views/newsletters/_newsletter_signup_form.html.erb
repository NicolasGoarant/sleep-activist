<div class="newsletter-cta-box">
  <h3>📧 Inscris-toi à la newsletter</h3>
  <p>Reçois chaque mois nos conseils, astuces et découvertes directement dans ta boîte mail.</p>

  <%# Modification : Utilisation de form_with pour pointer vers votre application Rails %>
  <%= form_with model: Subscriber.new, url: subscribers_path, method: :post, class: "newsletter-form", local: true do |f| %>
    <div class="form-group">
      <%= f.email_field :email,
          placeholder: "ton@email.com",
          required: true,
          class: "newsletter-input" %>

      <%= f.submit "S'inscrire gratuitement", class: "cta-button" %>
    </div>
    <p class="newsletter-disclaimer">
      Pas de spam, désinscription en 1 clic. Consulte notre
      <a href="/mentions-legales">politique de confidentialité</a>.
    </p>
  <% end %>
</div>
